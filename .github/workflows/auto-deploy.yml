# This is a basic workflow to help you get started with Actions
name: Auto merge and deploy files by schedules

# Controls when the workflow will run
on:
  repository_dispatch:
    types: [trigger-thtt-from-cronjob]
    
  workflow_dispatch: # Allows you to run the action manually for testing
  
permissions:
  contents: write # Required to push changes back to the repo
  pages: write # Required to deploy github pages
  id-token: write # Required to deploy github pages

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: github
  cancel-in-progress: true
  
jobs:
  merge-commit-and-deploy:
    runs-on: ubuntu-latest
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Start the job
      - name: Start the job
        run: |
          echo Hello, world! 

      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Checkout code
        uses: actions/checkout@v4
          
      - name: Set up Python 3.11
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests tqdm aiohttp lxml pysocks selenium-wire
      - name: Show current time
        run: |
          echo "Current datetime: $(date -u +'%Y-%m-%dT%H:%M:%SZ')"
          python helloworld.py
          git pull
          
      - name: Download file via curl
        run: |
          curl -s -k -H "Authorization: token ${{ secrets.MY_SECRET_PAT }}" -L -o ${{ secrets.NAME_THETHAO }} ${{ secrets.URL_PRIVATE_FILES_REPO }}/${{ secrets.NAME_THETHAO }}  & 
          curl -s -k -H "Authorization: token ${{ secrets.MY_SECRET_PAT }}" -L -o xoilacztv.py ${{ secrets.URL_PRIVATE_FILES_REPO }}/python/xoilacztv.py  & 
          curl -s -k -H "Authorization: token ${{ secrets.MY_SECRET_PAT }}" -L -o json2m3u.py ${{ secrets.URL_PRIVATE_FILES_REPO }}/python/json2m3u.py & 
          curl -s -k -H "Authorization: token ${{ secrets.MY_SECRET_PAT }}" -L -o extractm3u.py ${{ secrets.URL_PRIVATE_FILES_REPO }}/python/extractm3u.py & 
          curl -s -k -H "Authorization: token ${{ secrets.MY_SECRET_PAT }}" -L -o removem3u.py ${{ secrets.URL_PRIVATE_FILES_REPO }}/python/removem3u.py & 
          curl -s -k -H "Authorization: token ${{ secrets.MY_SECRET_PAT }}" -L -o apisports.py ${{ secrets.URL_PRIVATE_FILES_REPO }}/python/apisports.py & 
          curl -s -k -H "Authorization: token ${{ secrets.MY_SECRET_PAT }}" -L -o antlivetv.py ${{ secrets.URL_PRIVATE_FILES_REPO }}/python/antlivetv.py & 
          curl -s -k -H "Authorization: token ${{ secrets.MY_SECRET_PAT }}" -L -o fixjson.py ${{ secrets.URL_PRIVATE_FILES_REPO }}/python/fixjson.py & 
          curl -s -k -H "Authorization: token ${{ secrets.MY_SECRET_PAT }}" -L -o testproxies.py ${{ secrets.URL_PRIVATE_FILES_REPO }}/python/testproxies.py & 
          curl -s -k -H "Authorization: token ${{ secrets.MY_SECRET_PAT }}" -L -o keys.json ${{ secrets.URL_PRIVATE_FILES_REPO }}/jsonfiles/keys.json & 
          
          # download previous m3u files, for next use
          # curl -s -k -L -o xoilacztv.m3u https://thcoban.github.io/ththethao/xoilacztv.m3u  &
          # curl -s -k -L -o quechoatv.m3u https://thcoban.github.io/ththethao/quechoatv.m3u  &
          # curl -s -k -L -o tieulamtv.m3u https://thcoban.github.io/ththethao/tieulamtv.m3u  &
          # curl -s -k -L -o hoiquantv.m3u https://thcoban.github.io/ththethao/hoiquantv.m3u  &
          # curl -s -k -L -o khandaiatv.m3u https://thcoban.github.io/ththethao/khandaiatv.m3u  &
          # curl -s -k -L -o xaycontv.m3u https://thcoban.github.io/ththethao/xaycontv.m3u  &
          # curl -s -k -L -o colatv.m3u https://thcoban.github.io/ththethao/colatv.m3u  &
          # curl -s -k -L -o bugiotv.m3u https://thcoban.github.io/ththethao/bugiotv.m3u  &
          # curl -s -k -L -o tamquoctv.m3u https://thcoban.github.io/ththethao/tamquoctv.m3u  &
          # curl -s -k -L -o cakhiatv.m3u https://thcoban.github.io/ththethao/cakhiatv.m3u  &
          # curl -s -k -L -o nauxoitv.m3u https://thcoban.github.io/ththethao/nauxoitv.m3u  &
          # curl -s -k -L -o chuoichientv.m3u https://thcoban.github.io/ththethao/chuoichientv.m3u  &
          # curl -s -k -L -o luongsontv.m3u https://thcoban.github.io/ththethao/luongsontv.m3u  &
          # curl -s -k -L -o chaogatv.m3u https://thcoban.github.io/ththethao/chaogatv.m3u  &
          # curl -s -k -L -o vuasancotv.m3u https://thcoban.github.io/ththethao/vuasancotv.m3u  &
          # curl -s -k -L -o biaomtv.m3u https://thcoban.github.io/ththethao/biaomtv.m3u  &
          # curl -s -k -L -o s8tv.m3u https://thcoban.github.io/ththethao/s8tv.m3u  &
          # curl -s -k -L -o antlivetv.m3u https://thcoban.github.io/ththethao/antlivetv.m3u  &
          
          wait
          ls -lh

      - name: Download, get & convert links to M3U files
        run: |
          # if [ -f testproxies.py ]; then
          #   python testproxies.py etc/proxies.txt &
          # fi
        
          if [ -f xoilacztv.py ]; then
            # will overwrite previous xoilacztv, if the condition matches
            # python xoilacztv.py https://xoilacz.vip xoilacztv.m3u  & 
            python xoilacztv.py https://xoilacz.io xoilacztv.m3u  & 
          fi

          if [ -f antlivetv.py ]; then
            python antlivetv.py https://gavanglink.org antlivetv.m3u &
          fi
          
          if [ -f apisports.py ]; then
            python apisports.py cakhia cakhiatv.m3u &
            python apisports.py nauxoi nauxoitv.m3u &
            python apisports.py tieulam tieulamtv.m3u &
            python apisports.py luongson luongsontv.m3u &
            python apisports.py socolive socolivetv.m3u &
            
            python apisports.py khandaia khandaiatv.m3u &
            python apisports.py hoiquan hoiquantv.m3u &
            python apisports.py xaycon xaycontv.m3u &
            # python apisports.py chaoga chaogatv.m3u &
            python apisports.py cola colatv.m3u &
            python apisports.py bugio bugiotv.m3u &
            python apisports.py tamquoc tamquoctv.m3u &
            python apisports.py chuoichien chuoichientv.m3u &
            python apisports.py quechoa quechoatv.m3u &
            python apisports.py vuasanco vuasancotv.m3u &
            python apisports.py biaom biaomtv.m3u &
            python apisports.py s8tv s8tv.m3u &
          fi
          
          
          # if [ -f extractm3u.py ]; then
          #   # python extractm3u.py pktv.m3u new_pktv.m3u "Cháo Gà"  & 
          #   # python extractm3u.py vatv.m3u new_vatv.m3u "ColaTV"   &
            
          #   [ -f bongda.m3u ] && cp bongda.m3u bongda2.m3u
          #   python extractm3u.py bongda2.m3u socolivetv.m3u "socolive"
          #   # python extractm3u.py bongda3.m3u luongsontv.m3u "Lương sơn" 
          # fi
          
          # if [ -f removem3u.py ]; then
          #   python removem3u.py bongda.m3u new_bongda.m3u "Khán đài a, Gà vàng, Chuối Chiên" & 
          # fi
          
          # [ -f tlgsport.json ] && cp tlgsport.json ttthethao.json
          
          wait
      
      # Runs a set of commands using the runners shell
      - name: Merge Files
        # Combine base.m3u, tt.m3u and other m3u files into full.m3u
        run: |
          # delete old full.m3u, tt.m3u before merge
          rm -f ${{ secrets.NAME_THTHETHAO }}
          rm -f ${{ secrets.NAME_TTTHETHAO }}
          mkdir -p src
          ls -lh

          if [ -f nguyenban.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat nguyenban.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi
          if [ -f quechoatv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat quechoatv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi
          if [ -f khandaiatv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat khandaiatv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi
          if [ -f hoiquantv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat hoiquantv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi
          if [ -f bugiotv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat bugiotv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi
          if [ -f xaycontv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat xaycontv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi
          if [ -f chaogatv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat chaogatv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi
          if [ -f chuoichientv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat chuoichientv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi
          if [ -f vuasancotv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat vuasancotv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi
          if [ -f colatv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat colatv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi
          if [ -f luongsontv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat luongsontv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi
          if [ -f nauxoitv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat nauxoitv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi
          if [ -f s8tv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat s8tv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi
          if [ -f xoilacztv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat xoilacztv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi         
          if [ -f tieulamtv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat tieulamtv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi
          if [ -f tamquoctv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat tamquoctv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi
          if [ -f cakhiatv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat cakhiatv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi
          if [ -f biaomtv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat biaomtv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi

          if [ -f antlivetv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat antlivetv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi

          if [ -f socolivetv.m3u ]; then
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
            cat socolivetv.m3u >> ${{ secrets.NAME_TTTHETHAO }}
            echo " " >> ${{ secrets.NAME_TTTHETHAO }}
          fi
          

          cat ${{ secrets.NAME_THETHAO }} > ${{ secrets.NAME_THTHETHAO }}
          echo " " >> ${{ secrets.NAME_THTHETHAO }}
          cat ${{ secrets.NAME_TTTHETHAO }} >> ${{ secrets.NAME_THTHETHAO }}
          echo " " >> ${{ secrets.NAME_THTHETHAO }}
          # ls -lh

          echo "Last updated: $(date -u +'%Y-%m-%dT%H:%M:%SZ')" > lastupdated.txt

          # [ -f bongda.m3u ] && cp bongda.m3u src/
          
          [ -f ${{ secrets.NAME_THTHETHAO }} ] && cp ${{ secrets.NAME_THTHETHAO }} src/
          [ -f ${{ secrets.NAME_THETHAO }} ] && cp ${{ secrets.NAME_THETHAO }} src/
          [ -f ${{ secrets.NAME_TTTHETHAO }} ] && cp ${{ secrets.NAME_TTTHETHAO }} src/

          # keep current xoilacztv.m3u for next use
          # [ -f xoilacztv.m3u ] && cp xoilacztv.m3u src/
          # [ -f quechoatv.m3u ] && cp quechoatv.m3u src/
          # [ -f hoiquantv.m3u ] && cp hoiquantv.m3u src/
          # [ -f khandaiatv.m3u ] && cp khandaiatv.m3u src/
          # [ -f xaycontv.m3u ] && cp xaycontv.m3u src/
          # [ -f colatv.m3u ] && cp colatv.m3u src/
          # [ -f bugiotv.m3u ] && cp bugiotv.m3u src/
          # [ -f tamquoctv.m3u ] && cp tamquoctv.m3u src/
          # [ -f chuoichientv.m3u ] && cp chuoichientv.m3u src/
          # [ -f cakhiatv.m3u ] && cp cakhiatv.m3u src/
          # [ -f nauxoitv.m3u ] && cp nauxoitv.m3u src/
          # [ -f quechoatv.m3u ] && cp quechoatv.m3u src/
          # [ -f tieulamtv.m3u ] && cp tieulamtv.m3u src/
          # [ -f luongsontv.m3u ] && cp luongsontv.m3u src/
          # [ -f chaogatv.m3u ] && cp chaogatv.m3u src/
          # [ -f vuasancotv.m3u ] && cp vuasancotv.m3u src/
          # [ -f biaomtv.m3u ] && cp biaomtv.m3u src/
          # [ -f s8tv.m3u ] && cp s8tv.m3u src/
          # [ -f antlivetv.m3u ] && cp antlivetv.m3u src/
          # [ -f nguyenban.m3u ] && cp nguyenban.m3u src/
          
          # [ -f socolivetv.m3u ] && cp socolivetv.m3u src/
          
          [ -f keys.json ] && cp keys.json src/
          # [ -f ttthethao.json ] && cp ttthethao.json src/         
          
          # ls -lh src/

          
      - name: Setup Pages
        uses: actions/configure-pages@v5
        
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          retention-days: 0
          # Upload in src/ folder only
          path: 'src/'

      - name: Retry Deployment on Failure
        id: deployment
        uses: Wandalen/wretry.action@v3
        with:
          action: actions/deploy-pages@v5  # Swap this out for your specific deployment action
          attempt_limit: 3
          attempt_delay: 3
    
      # - name: Deploy to GitHub Pages
      #   id: deployment
      #   uses: actions/deploy-pages@v5        

      - name: Delete artifact validation file
        uses: geekyeggo/delete-artifact@v6
        with:
          name: |
              github-pages.zip
              github-pages
          failOnError: false
      
      - name: Commit and push changes
        run: |
          git config --local user.name "thcoban [bot]"
          git add lastupdated.txt
          git add etc/testing/host.txt

          echo "checking any changes"
          if git diff --staged --quiet; then
            echo "[test only] No changes detected."
          fi
          
          git pull
          git commit --allow-empty -m "[no ci] Update repo" && git push
